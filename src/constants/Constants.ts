export const SYSTEM_MODE: string = import.meta.env.VITE_APP_MODE;

export const BASE_URL: string = import.meta.env.VITE_APP_BASE_URL; // http://localhost:8000
export const API_BASE_URL: string = `${BASE_URL}/api`; // http://localhost:8000/api
